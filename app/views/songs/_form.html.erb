<%= form_for(song, {method: method, url: url}) do |f| %>
  <% if song.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(song.errors.count, 'error') %> prohibited this song from being saved:</h2>

      <ul>
      <% song.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :album_title %>
    <%= f.text_field :album_title %>
  </div>

  <div class="field">
    <%= f.label :track_number %>
    <%= f.number_field :track_number %>
  </div>

  <div class="field">
    <%= f.label :length %>
    <%= f.number_field :length %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
